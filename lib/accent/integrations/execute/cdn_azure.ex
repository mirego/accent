defmodule Accent.IntegrationManager.Execute.CdnAzure do
  @moduledoc false
  def upload_translations(_integration, _params) do
    # Generate translation export files for the given project and given version (latest or specific tag)
    # Generate Azure signature to upload
    # Upload the files to Azure CDN
    :ok
  end
end
