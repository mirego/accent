{{#unless this.translation.removed}}
  <TranslationIndex>
    <TranslationEdit
      @translation={{this.model.translation}}
      @prompts={{this.model.prompts}}
      @project={{this.model.project}}
      @revisions={{this.model.revisions}}
      @permissions={{this.permissions}}
      @onUpdateText={{fn this.updateText}}
      @onCorrectConflict={{fn this.correctConflict}}
      @onUncorrectConflict={{fn this.uncorrectConflict}}
    />

    {{#if this.model.relatedTranslations}}
      {{#if (get this.permissions 'index_related_translations')}}
        <RelatedTranslationsList
          @project={{this.model.project}}
          @permissions={{this.permissions}}
          @revisions={{this.model.revisions}}
          @prompts={{this.model.prompts}}
          @translations={{this.model.relatedTranslations}}
          @onUpdateText={{fn this.updateTranslation}}
        />
      {{/if}}
    {{/if}}
  </TranslationIndex>
{{else}}
  <RemovedTranslationEdit @translation={{this.model.translation}} />
{{/unless}}