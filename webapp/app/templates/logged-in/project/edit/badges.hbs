<ProjectSettings::BackLink @project={{this.project}} />
{{#if this.showLoading}}
  <LoadingContent @label={{t 'pods.project.edit.badges.loading_content'}} />
{{else}}
  <ProjectSettings::Badges @project={{this.project}} />
{{/if}}