<AccModal @small={{true}} @onClose={{fn this.closeModal}}>
  <VersionUpdateForm
    @version={{this.version}}
    @error={{this.error}}
    @project={{this.project}}
    @onUpdate={{fn this.update}}
  />
</AccModal>
