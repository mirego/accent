<AccModal @small={{true}} @onClose={{action "closeModal"}}>
  <VersionUpdateForm
    @version={{version}}
    @error={{error}}
    @project={{project}}
    @onUpdate={{action "update"}}
  />
</AccModal>
