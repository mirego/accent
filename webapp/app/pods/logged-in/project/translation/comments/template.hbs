{{#if this.model.loading}}
  <SkeletonUi::ProgressLine />
{{/if}}

{{#if this.showSkeleton}}
  <SkeletonUi::TranslationCommentsList />
{{else}}
  <TranslationConversation
    @permissions={{this.permissions}}
    @translation={{this.model.translation}}
    @collaborators={{this.model.collaborators}}
    @subscriptions={{this.model.commentsSubscriptions}}
    @comments={{this.model.comments}}
    @onCreateSubscription={{fn this.createSubscription}}
    @onDeleteSubscription={{fn this.deleteSubscription}}
    @onSubmit={{fn this.createComment}}
    @onSelectPage={{fn this.selectPage}}
  />
{{/if}}
