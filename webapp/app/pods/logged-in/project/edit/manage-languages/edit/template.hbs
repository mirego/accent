{{#if this.revision}}
  {{#acc-modal small=true onClose=(action 'closeModal')}}
    {{revision-update-form
      project=this.project
      revision=this.revision
      onUpdate=(action 'update')
    }}
  {{/acc-modal}}
{{/if}}
