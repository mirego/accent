{{#if showLoading}}
  <LoadingContent @label={{t "pods.project.edit.api_token.loading_content"}} />
{{else}}
  <ProjectSettings::BackLink @project={{project}} />

  {{#if project.accessToken}}
    <ProjectSettings::ApiToken @token={{project.accessToken}} />
  {{/if}}
{{/if}}
