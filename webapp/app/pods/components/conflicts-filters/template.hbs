<div class="filters">
  <form {{action 'submitForm' on='submit'}} class="filters-wrapper">
    <div class="filters-content">
      <div class="queryForm">
        {{inline-svg '/assets/search.svg' class='search-icon'}}

        {{input
          class='input'
          type='text'
          placeholder=(t 'components.conflicts_filters.input_placeholder_text')
          value=debouncedQuery
        }}
      </div>

      <div class="queryForm-filters">
        {{#if showDocumentsSelect}}
          <div class="queryForm-filter">
            <div class="queryForm-filter-select">
              {{#power-select
                searchEnabled=false
                selected=documentValue
                options=mappedDocuments
                onchange=(action onChangeDocument value='value') as |document|
              }}
                {{document.label}}
              {{/power-select}}
            </div>
          </div>
        {{/if}}

        {{#if showReferenceRevisionsSelect}}
          <div class="queryForm-filter">
            <div class="queryForm-filter-select">
              {{#power-select
                searchEnabled=false
                selected=referenceValue
                options=mappedReferenceRevisions
                onchange=(action onChangeReference value='value') as |reference|
              }}
                {{reference.label}}
              {{/power-select}}
            </div>
          </div>
        {{/if}}
      </div>
    </div>

    {{#if meta.totalEntries}}
      <span class="totalEntries">
        {{t 'components.conflicts_filters.total_entries_count' count=meta.totalEntries}}
      </span>
    {{/if}}
  </form>
</div>
